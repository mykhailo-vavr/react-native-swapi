import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    display: 'flex',
    gap: 6,
    padding: 10,
    borderBottomColor: '#ccc',
    borderBottomWidth: 2,
    backgroundColor: '#eee',
    marginBottom: 10,
    borderRadius: 4
  }
});
