import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  emptyWrapper: {
    display: 'flex',
    alignItems: 'center',
    paddingTop: 20
  },
  emptyText: {
    fontWeight: 'bold'
  }
});
