import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  sortWrapper: {
    display: 'flex',
    gap: 10,
    paddingTop: 14,
    paddingBottom: 14
  },
  sortLabel: {
    fontSize: 16
  }
});
